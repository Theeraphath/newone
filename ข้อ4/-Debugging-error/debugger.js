 // กำหนดค่าให้กับตัวแปร
 let a = 5;
 let b = 10;

 // การหยุดที่บรรทัดนี้
 debugger;

 // การคำนวณผลลัพธ์
 let result = a + b;

 // แสดงผลลัพธ์ในคอนโซล
 console.log("ค่าของ a:", a);  // ค่าของ a
 console.log("ค่าของ b:", b);  // ค่าของ b
 console.log("ผลลัพธ์:", result);  // ผลลัพธ์ของการคำนวณ